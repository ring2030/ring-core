import { ScreenRecordDemo } from "@/components/kiyoko/ScreenRecordDemo";

/** Same UI as /demo (alternate URL if /demo is blocked). */
export default function RecordDemoAliasPage() {
  return <ScreenRecordDemo />;
}
